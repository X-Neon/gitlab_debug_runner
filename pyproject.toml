[project]
name = "gdr"
version = "0.1.0"
description = "Gitlab Debug Runner"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "docker>=7.1.0",
    "python-gitlab>=5.6.0",
    "pyyaml>=6.0.2",
]
